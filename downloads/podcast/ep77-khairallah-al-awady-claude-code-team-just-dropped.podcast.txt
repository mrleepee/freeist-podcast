Did you know that when most developers start building eh! eye! agents, they underestimate just how much infrastructure is needed to make them actually work in production? It's not just about writing a clever system prompt and hooking up an eh! pee! eye!. There's the agent loop logic, retry mechanisms, error recovery, session management, and then you've got to think about how multiple agents coordinate with each other. Suddenly you're running a small infrastructure company rather than building your actual product. That's exactly the problem Cloud Manager Agents sets out to solve.

Cloud Manager Agents is essentially a production-ready platform that gives you all the building blocks for building eh! eye! agent products without having to reinvent the wheel. It handles everything from scalable infrastructure to tool calling, retries, error recovery, memory, context management, and multi-agent coordination. Everything comes with built-in observability through a developer console, so you can see exactly what's happening as your agents work.

Let's look at the core components. Agents are essentially templates where you define your custom system prompts, skills, and tool access. You can connect them to em! see! pee! servers, which is the Model Context Protocol, and you get granular permission controls for each tool. Some tools can auto-execute while others require human approval before running. Environments let you define how your containers behave, controlling network access and specifying pre-installed packages. The really interesting bit here is support for self-hosted sandboxes, so you can use Cloudflare, Modal, Vercel, or even your own custom infrastructure. Sessions represent ongoing conversations with Cloud, and you can preload GitHub repositories and files directly into them.

The event system is where things get quite sophisticated. You've got user events covering messages, interrupts, tool results, confirmations, and outcomes. Agent events handle responses, context compaction, tool execution, and multi-agent coordination. Session events track lifecycle and state transitions, while span events monitor long-running tasks. It gives you a really complete picture of everything happening in your agent workflows.

Two new features stood out to me. First, self-hosted sandboxes mean you can bring your own containers for handling private data, which is crucial for enterprise use cases. Second, em! see! pee! tunnels provide secure connections to private em! see! pee! servers without exposing anything to the internet. That's a really thoughtful addition for companies with strict security requirements.

The benefits over building from scratch are substantial. You don't need to construct agent loops or host infrastructure. The platform manages context and state transitions automatically. There's durable storage for events and agent data, built-in end-user authentication, and a secure credential vault for em! see! pee! tokens. All the tedious plumbing is taken care of.

To see this in action, the demo showed a deal desk product for em! and! eh! investment decisions, using a multi-agent architecture with specialised sub-agents handling different aspects of the analysis. They demonstrated live session monitoring in the developer console and outcome-based task evaluation, which really showed how all these pieces fit together in a real application.

What's becoming clear is that the infrastructure question for eh! eye! agents is rapidly being solved by platforms like this. The exciting part is what developers can now build on top of that foundation.