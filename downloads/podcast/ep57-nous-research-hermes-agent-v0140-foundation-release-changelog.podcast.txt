Imagine having a conversation where you could reference a million words without losing the thread — well, the latest release of this eh! eye! agent framework has just made that a reality. Hermes Agent version zero point one four has dropped, and it's packed with updates that make it more powerful than ever.

The headline feature is GROC four point three, which now supports a one million token context window. That's enormous — it means the system can process and retain far more information in a single conversation than most other tools on the market. For developers building complex workflows or researchers working with large documents, this is a game-changer. Installation has been streamlined too, with one-click setup through the agent client protocol registry, and there's now a pee! why! pee! eye! distribution available with a you! vee! ex! install path option that makes getting started even easier.

Now, let's talk about connectivity because this release is all about getting the agent to work wherever you need it. Direct ex, formerly known as Twitter, can now be searched directly from the agent without any awkward integration steps — you just ask and it fetches what you need. The list of messaging platforms has grown again too. Line and Simplex have just joined the party, bringing the total to twenty-two different services you can chat through. And the Teams integration deserves a special mention because it's proper end-to-end now — the agent can read and post across channels, group chats, and direct messages, making it genuinely useful in a workplace setting.

For the more technical listeners, there's some good news on the runtime front. An optional Codex runtime has been added for OpenAI's Codex CLI, and computer use capabilities have been expanded to work with any model, not just Propix. There's also a unified video-generating tool with swappable model plugins, which opens up some interesting possibilities for content creation workflows. Native Windows support has arrived in early beta too, and this one matters — it works directly with cmd dot exe and PowerShell without needing WSL, so Windows users can finally get going without additional setup.

One clever new feature is called slash subgoals. The idea is simple but effective — while a goal is running, you can add extra criteria on the fly without restarting the whole process. The Judge component picks up these additions automatically without disrupting the loop, which makes iterative refinement much smoother.

Three more optional skills have joined the roster, taking the total to nine. Yahoo Finance brings market data directly into the workflow, Notion support opens up the developer platform side of things, and there's dedicated API testing for both REST slash GraphQL debugging.

Version zero point one four is available right now, so if any of this sounds useful for your projects, it's worth taking a look. The direction here is clear — bigger context, wider connectivity, and more flexibility for different use cases. It's shaping up to be a versatile platform for anyone building with eh! eye! agents.